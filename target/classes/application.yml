spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.241.0.41:3306/bbs?serverTimezone=UTC
    username: root
    password: bkjk@2018
    initialization-mode: always
  jpa:
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
  thymeleaf:
    cache: false

logging:
  level:
    cn.edu.ncu.jiluan.bbs: debug
